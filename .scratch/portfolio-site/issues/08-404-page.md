# On-brand 404 page

Status: done

## Parent

`.scratch/portfolio-site/PRD.md`

## What to build

`src/pages/404.astro` reusing `BaseLayout` and design tokens. Minimal: short message, link back to `/`. Matches the rest of the site's typography and palette so a mistyped URL doesn't read as broken.

## Acceptance criteria

- [x] `src/pages/404.astro` exists and reuses `BaseLayout`
- [x] Visually matches the rest of the site (tokens, fonts, palette)
- [x] Includes a link back to `/`
- [x] Verified by visiting an unknown route in dev

## Blocked by

- `03-baselayout-meta-builder.md`
